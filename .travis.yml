language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-test4' ]; then cd .. && eval "mv $currentfolder generator-test4" && cd generator-test4; fi

